class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        c=0
        for i in nums:
            c+=1
        for i in range(0,c+1):
            if i not in nums:
                return i
